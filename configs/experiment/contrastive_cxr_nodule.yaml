# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - _self_
  - override /module: model/contrastive
  - override /callbacks:
      - learning_rate_monitor
      - online_eval
      - checkpoint/pretrain
#      - online_cluster
  

test: False

val_transform:
  _target_: data.augmentation.ThreeCropTransform
  _args_:
    - _target_: data.augmentation.MedicalGreyScaleTrainTransform
train_transform:
  _target_: data.augmentation.ThreeCropTransform
  _args_:
    - _target_: data.augmentation.MedicalGreyScaleTrainTransform
